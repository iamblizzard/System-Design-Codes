#include "sorting-strategy.h"

void BubbleSort::sort(int* array, int size) {
    std::cout << "Sort using BubbleSort" << std::endl;
}

void QuickSort::sort(int* array, int size) {
    std::cout << "Sort using QuickSort" << std::endl;
}

void MergeSort::sort(int* array, int size) {
    std::cout << "Sort using MergeSort" << std::endl;
}